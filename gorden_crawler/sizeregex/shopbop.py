#-*-coding:utf-8-*-
import re
from gorden_crawler.sizeregex.basestrategy import BaseStrategy

class Shopbop (BaseStrategy):

    def transform(self, size, reg, site, key):
        return
    
    
    def getReMap(self):
        
        return